.claude/
.relay/
CLAUDE.local.md
.venv/
__pycache__/
*.egg-info/
dist/
build/
.gpt-pro-cookies*.json
# Session token exports (secret — never commit; may be dropped in the repo root
# without a leading dot, e.g. gpt-pro-token-YYYYMMDD.json).
gpt-pro-token*.json
.gpt-pro-token*.json
.gpt-pro-env.sh
# Private research archive — a separate private repo cloned into research/.
# Never committed to this public repo (topics stay internal). See README.
research/
# Run artifacts / state (GPT_PRO_RUNS_DIR / GPT_PRO_HOME) — never commit these:
# they hold prompts, responses, chat URLs, and screenshots.
runs/
.gpt-pro-state/
